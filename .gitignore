node_modules/

# Secrets — Supabase service role key, for the owner's admin tooling only.
# A user's own push token lives in ~/.deal-finder/env, outside the repo.
.env.local
.env

# Your own searches config. The real copy lives on the server now; this is only
# a local scratch copy and it names your city, budgets and what you hunt.
searches.json

*.log
.DS_Store

# Personal Claude Code permissions for this machine.
.claude/settings.local.json
